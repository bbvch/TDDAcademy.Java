# Description

Bootstrap project for fast kickoff. Java

- Library
- Unit and spec tests

## Run tests

```shell
./gradlew test
```

To run them again (without changing code) add `--rerun`
